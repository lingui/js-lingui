machine:
  node:
    version: 6.1.0

dependencies:
  pre:
    - yarn global add lerna codecov
  override:
    - yarn install
    - lerna bootstrap

test:
  override:
    - ./scripts/circleci-test.sh:
      parallel: true
  post:
    - codecov
