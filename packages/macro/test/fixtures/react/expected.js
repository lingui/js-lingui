const T = <Trans>Read <a href="/more" title={/*i18n*/{
    id: "Full content of {articleName}",
    values: {
      articleName: articleName
    }
  }}>more</a></Trans>;

const P = <a href="/about" title={/*i18n*/{
  id: "{count, plural, one {# book} other {# books}}",
  values: {
    count: count
  }
}}>About</a>;
