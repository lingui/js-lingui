import { t } from "@lingui/core/macro"
import { msg as headerMsg } from "../components/header"
import { GREEN } from "../constants"

const msg = t`about page message`

console.log(msg, headerMsg)
console.log(GREEN)
